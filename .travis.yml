# Sample .travis.yml for R projects

language: r
warnings_are_errors: true
sudo: required

services:
  - docker

r_packages:
  - covr
  - raster
  - maps
  - permute
  - gtools
  - quadprog

after_success:
  - Rscript -e 'library(covr); codecov()'
